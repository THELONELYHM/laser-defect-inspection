model:
  weights: models/best.pt
  image_size: 640
  confidence: 0.25
  iou: 0.45
  device: cpu

inspection:
  use_clahe: false
  clahe_clip_limit: 2.0
  clahe_grid_size: 8
  max_allowed_defects: 0
  min_defect_area_mm2: 0.0
  mm_per_pixel: null

output:
  directory: outputs/inference
  save_image: true
  save_json: true

